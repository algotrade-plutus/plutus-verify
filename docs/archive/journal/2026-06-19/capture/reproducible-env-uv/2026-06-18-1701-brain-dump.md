---
feature: reproducible-env-uv
type: brain-dump
date: 2026-06-18
time: 17:01
source-skill: hook:precompact
tags: [auto, precompact, enriched]
---

# Brain Dump — precompact

## Why this dump
precompact — Claude Code is about to compact context. Auto-written by the PreCompact hook to preserve working state. The narrative sections below are populated by the LLM on the first post-compact turn via the SessionStart hook.

## Recent decisions
Snapshot taken right after the `reproducible-env-uv` 0.4.0 cycle was wrapped and committed (`0837739` "Document and archive the knowledge"). At this point the feature was considered done: B1 (verify-side `env.manager: uv` + `env.lockfile`, lockfile restored with `uv sync --frozen` into `/opt/venv` outside the staging mount, warn-only `env: NOT reproducible` seam) and B2 (`plutus-transform` reframed to uv-lock-first) shipped, `journal:document` + `journal:archive` run, the cycle archived to `docs/archive/journal/2026-06-18/`. Working memory held it as a clean slate.

_(Enriched post-hoc during the 0.4.1 capture: the "clean slate" assumption did not hold — a runtime bug arrived immediately after. See the 0.4.1 bug-fix capture: the uv path built green but every step failed at runtime because the runner used a login shell that reset PATH and hid the `/opt/venv` venv.)_

## Open threads
- Working on branch: `main`
- `main` was ahead of `origin` and awaiting a user push at compaction time.
- A uv-runner runtime bug report (`plutus-verify-uv-runner-bug.md`) from the 9-step dev-tooling session was about to land — became the 0.4.1 fix.

## Files currently in flight
_None._

## Recent commits
```
0837739 Document and archive the knowledge
eb6b2f0 feat: plutus-transform port-to-uv + release 0.4.0
2e854b5 feat: verify uv-locked environments (env.manager + env.lockfile)
a5f9739 chore: release 0.3.0
2b13b43 feat: v2025 nine-step taxonomy + data_preparation sub_processes
6fde5b8 Update README.md
802855e Restructure technical document
e77d50f feat: v0.2.7 → v0.2.10 — leak-closure arc + skill split
60e4156 feat: v0.2.6 — schema honesty, artifacts rename, visual_similarity opt-in
26d9162 simplify: pass C — efficiency + rename plutus_verify/build → builder
```

## Notes
_Auto-written by PreCompact hook. Narrative enrichment happens automatically on the first post-compact turn via the SessionStart hook. Convert any new insights into `decision` or `bug-fix` captures if appropriate._
