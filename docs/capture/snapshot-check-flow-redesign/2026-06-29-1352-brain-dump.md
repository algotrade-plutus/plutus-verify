---
feature: snapshot-check-flow-redesign
type: brain-dump
date: 2026-06-29
time: 13:52
source-skill: hook:precompact
tags: [auto, precompact, enriched]
---

# Brain Dump — precompact

## Why this dump
precompact — Claude Code is about to compact context. Auto-written by the PreCompact hook to preserve working state. The narrative sections below are populated by the LLM on the first post-compact turn via the SessionStart hook.

## Recent decisions
The snapshot/check-flow redesign cycle is closed: the feature-complete capture
(`docs/capture/snapshot-check-flow/2026-06-29-1338-complete.md`) was written and the
two-verb/three-store read-only model shipped (0.5.0 code, 0.5.1 docs/skills, 554 passed).
The terminal action for that feature is done; remaining cycle work is `/journal:document`
then `/journal:archive` (5 captures pending across snapshot-check-flow, sdk-install,
plutus-standardize, and this redesign dir).

New thread opened post-compact (this segment): **GT10 feedback from the PlutusSmartBeta /
ProtoSmartBeta transform run** (`skill-feedback.md`). A real divergence fired — the
`plutus-transform` skill's GT10 tells the operator to install the `runner` extra (for
`gdown`) into "the dev venv", but on that run `plutus` was installed as a `uv tool` (global
isolated venv at `~/.local/share/uv/tools/plutus-verify/`), so installing gdown into the
**project `.venv`** did nothing — the Drive fetch still failed with `No module named
'gdown'`. Verified the root cause in code: `data_resolver._download_google_drive` →
`fetch._default_gdown_folder/_file` imports gdown **in the host process that runs the CLI**
(not the container, not the project venv). So the correct target depends on how `plutus` is
installed: `uv tool install --force "plutus-verify[runner] @ $WHL"` for a tool/pipx install,
vs `uv pip install ...[runner]` only when `plutus` is invoked from the project venv
(`uv run plutus`). The feedback's draft GT10 addendum is sound; pending decision is whether
to promote it into `skills/plutus-transform/references/known-gotchas.md` (GT10) and the
Phase 5 step-2 prose. User's mental model ("gdown goes in the transformed project's venv")
is the exact misconception the addendum corrects.

## Hypotheses / open questions
- Promote the GT10 addendum verbatim, or generalize it (the same "host-side fetch runs in
  the CLI's interpreter" rule also affects any host-side resolver dep, not just gdown)?
- The two notes in the feedback (GT4 SDK-floor, chart `visual_similarity` non-determinism)
  behaved as documented — no action, just confirmation the skill text is accurate.

## Open threads
- Working on branch: `main`
_No uncommitted changes at compaction time._

## Files currently in flight
_None._

## Recent commits
```
81a92c5 Drafting the plutus-transform skill
3e3ee7a Change skills based on feedback from the session of PlutusSmartBeta
c9b3561 release 0.5.1 (docs + skills; wheel code identical to 0.5.0)
14c3cbb docs: note env.install_project (0.5.0) in reproducible-env
249c0ac feat(skill): plutus-scoring v0.5.0 scoring notes (read-only check)
f7e6d94 feat(skill): plutus-standardize D6 install_project decision + template hints
4c3f4c1 feat(skill): plutus-standardize reflects 0.5.0 read-only check + cache + gitignore
a055571 feat(skill): plutus-standardize v0.5.0 version notes
49bfe76 docs(changelog): add 0.5.0 entry
a0a539e docs(plan): ship-readiness 0.5.0 docs + skill updates plan
```

## Notes
_Auto-written by PreCompact hook. Narrative enrichment happens automatically on the first post-compact turn via the SessionStart hook. Convert any new insights into `decision` or `bug-fix` captures if appropriate._
